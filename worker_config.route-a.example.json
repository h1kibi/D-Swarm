{
  "_comment": "Route-A container worker config. The roster contains only the Pi worker profiles available on this host; ReasonSwarm reads SharedGraph, schedules typed intents, and review/revalidation audits results.",
  "engines": ["pi-api-container"],
  "worker_backend": "container"
}
